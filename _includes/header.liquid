{%- if landing_page -%}

<div class="site-header landing">
    <div class="container d-flex justify-content-between mx-auto">
        <nav class="navbar navbar-dark">
          <a class="navbar-brand d-block" href="/"
            ><img class="logo" src="/img/logo.png" alt="Sales Rebellion Home page"
          ></a>
          </nav>  
          <div class="site-header__dates">
              <div class="site-header__dates--date-range">{{- dates.date_range -}}</div>
              <div class="site-header__dates--month-year">{{- dates.month_year | upcase -}}</div>
              <div class="site-header__dates--location">{{- location | upcase -}}</div>
          </div>
    </div>
</div>

{%- else -%}

<div class="site-header sticky-top">
  <nav class="navbar navbar-expand-lg navbar-dark bg-black mx-auto">
    <a class="navbar-brand d-block" href="/"
      ><img class="logo" src="/img/logo.png" alt="Sales Rebellion Home page"
    ></a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <img src="/img/svg/hamburger-icon.svg" alt="">
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
      <ul class="navbar-nav">
        {% for link in nav_links %}
          {% if link.dropdown_links and link.dropdown_links.size > 0 %}
            <li class="nav-item dropdown d-flex flex-column align-items-center">
              <a
                class="nav-link dropdown-toggle font-kufam"
                href="#"
                id="navbarDropdownMenuLink"
                data-bs-toggle="dropdown"
                aria-haspopup="true"
                aria-expanded="false"
              >
                {{ link.header }}
                <img src="/img/svg/chevron-down.svg" alt="">
              </a>
              <div
                class="dropdown-menu border-green"
                style="left: unset !important"
                aria-labelledby="navbarDropdownMenuLink"
              >
                {% for dropdown in link.dropdown_links %}
                  <a class="dropdown-item text-white font-kufam" href="{{ dropdown.link_url }}">
                    {{- dropdown.link_text -}}
                  </a>
                {% endfor %}
              </div>
            </li>
          {% else %}
            <li class="nav-item">
              <a class="nav-link font-kufam" href="{{ link.link_url }}">{{ link.header }}</a>
            </li>
          {% endif %}
        {% endfor %}
      </ul>
    </div>
  </nav>
</div>

{%- endif -%}